/* ============================================================
   站点配置 — 每个部署各自维护，互不关联
   ------------------------------------------------------------
   复用本页面时，只需修改这一个文件：
   - baseUrl：留空 = 自动用当前访问域名（同域反代场景，推荐）；
              或显式填写 API 域名以覆盖（不同域部署时）
   - brandName：顶栏品牌名。留空 = 只显示中性文案，不强调任何品牌
   - title：浏览器标签页标题
   - footerText：页脚文案
   ============================================================ */
window.SITE_CONFIG = {
  baseUrl: "",
  brandName: "",
  title: "API 接入文档",
  footerText: "客户接入文档"
};
